# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist

# Environment files
.env
.env.local
.env.*.local
dist-ssr
*.local

# Build output
build
coverage

# API generated files
api/generated
api/dist
api/node_modules

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tool configs
.cline/
.opencode/
release-manifest.json

# Claude session artifacts (command transcripts may contain sensitive values)
.claude/hooks/.logs/
.claude/session-state/
.claude/agent-memory/
api/.claude/
.playwright-mcp/

# Python bytecode cache
__pycache__/

# Dev/E2E screenshots + snapshot artifacts (root-level throwaway, not project source)
/*.png
/*-snapshot.md
dashboard-snapshot.md

# Large copyrighted reference material — kept locally, NOT distributed via git
# (the app extracts guidance content from it; the binary itself is not committed)
docs/the-big-book-of-endurance-training-and-racing.pdf
